<section id="viewSubmit">
    <div class="submitArea">
        <h1>Edit Link</h1>

        <p>Please, fill out the form. A thumbnail image is not required.</p>
    </div>
    <div class="submitArea formContainer">
        <form id="submitForm" class="submitForm" action="#/edit/:{{id}}" method="post">
            <label>Link URL:</label>
            <input name="url" value="{{url}}" type="text">
            <label>Link Title:</label>
            <input name="title" value="{{title}}" type="text">
            <label>Link Thumbnail Image (optional):</label>
            <input name="image" value="{{imageUrl}}" type="text">
            <label>Comment (optional):</label>
            <textarea name="comment">{{description}}</textarea>
            <input id="btnSubmitPost" value="Submit" type="submit">
        </form>
    </div>
</section>